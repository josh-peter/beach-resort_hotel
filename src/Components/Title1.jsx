import React from "react";

const Title = () => {
  const title = "Featured Rooms";

  return (
    <div className="section-title">
      <h4>{title}</h4>
      <div />
    </div>
  );
};

export default Title;
